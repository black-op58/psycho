<?xml version="1.0" encoding="utf-8"?>
<!--
    Compact media card — title overlaid inside cover at the bottom.
    Landscape (default): 160×90 dp  |  Portrait (pref): 110×160 dp.
    CardOrientation pref controls image size (set in adapter onCreateViewHolder).
    CardImageType pref controls whether cover or banner art is loaded.
    Watch progress bar shown at bottom edge when userProgress > 0.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="-16dp"
    android:layout_marginEnd="-16dp"
    android:clipToPadding="false"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="-16dp"
        android:layout_marginBottom="-16dp"
        android:clipToPadding="false"
        android:padding="22dp">

        <androidx.cardview.widget.CardView
            android:id="@+id/itemCompactCard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:translationZ="8dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp">

            <!-- Blurred cover fill — only shown in landscape+cover mode to avoid empty bars -->
            <ImageView
                android:id="@+id/itemCompactImageBg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:alpha="0.45"
                android:visibility="gone"
                tools:ignore="ContentDescription,ImageContrastCheck" />

            <!-- Cover/banner image — dimensions set by adapter (CardOrientation pref) -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/itemCompactImage"
                android:layout_width="160dp"
                android:layout_height="90dp"
                android:scaleType="centerCrop"
                android:transitionName="mediaCover"
                app:shapeAppearanceOverlay="@style/roundedImageView"
                tools:ignore="ContentDescription,ImageContrastCheck"
                tools:srcCompat="@tools:sample/backgrounds/scenic" />

            <!-- Bottom gradient scrim — keeps white title readable over any art -->
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="72dp"
                android:layout_gravity="bottom"
                android:scaleType="fitXY"
                app:srcCompat="@drawable/gradient_overlay_bottom"
                tools:ignore="ContentDescription" />

            <!-- Title + logo + episode progress count, pinned to bottom of cover -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="vertical"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingBottom="9dp">

                <ImageView
                    android:id="@+id/itemCompactLogoArt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:adjustViewBounds="true"
                    android:maxHeight="34dp"
                    android:scaleType="fitStart"
                    android:visibility="gone"
                    tools:ignore="ContentDescription" />

                <TextView
                    android:id="@+id/itemCompactTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:textColor="@android:color/white"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:shadowColor="#CC000000"
                    android:shadowDx="0"
                    android:shadowDy="1"
                    android:shadowRadius="4"
                    android:transitionName="mediaTitle"
                    tools:ignore="SpUsage"
                    tools:text="Demon Slayer: Kimetsu no Yaiba" />

                <!-- 3-part episode count: [watched primary] | [released white] | [total grey] -->
                <LinearLayout
                    android:id="@+id/itemCompactProgressContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/itemCompactUserProgress"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="?attr/colorPrimary"
                        android:textSize="10sp"
                        android:textStyle="bold"
                        tools:ignore="SpUsage,TextContrastCheck"
                        tools:text="7" />

                    <TextView
                        android:id="@+id/itemCompactReleased"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@android:color/white"
                        android:textSize="10sp"
                        tools:ignore="SpUsage,TextContrastCheck"
                        tools:text=" | 11" />

                    <TextView
                        android:id="@+id/itemCompactTotal"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:alpha="0.5"
                        android:textColor="@android:color/white"
                        android:textSize="10sp"
                        tools:ignore="SpUsage,TextContrastCheck"
                        tools:text=" | 12" />
                </LinearLayout>
            </LinearLayout>

            <!-- Score badge — top-end corner -->
            <LinearLayout
                android:id="@+id/itemCompactScoreBG"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:background="@drawable/item_score"
                android:backgroundTint="#FFFFFF"
                android:backgroundTintMode="multiply"
                android:orientation="horizontal"
                android:paddingStart="8dp"
                android:paddingEnd="6dp">

                <TextView
                    android:id="@+id/itemCompactScore"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="2dp"
                    android:fontFamily="@font/poppins_bold"
                    android:paddingTop="2dp"
                    android:textColor="?attr/colorOnPrimary"
                    android:textSize="12sp"
                    tools:ignore="TextContrastCheck"
                    tools:text="8.7" />

                <ImageView
                    android:id="@+id/imageView2"
                    android:layout_width="12dp"
                    android:layout_height="match_parent"
                    app:srcCompat="@drawable/ic_round_star_24"
                    app:tint="?attr/colorOnPrimary"
                    tools:ignore="ContentDescription" />
            </LinearLayout>

            <!-- "New Episode" badge — top-start corner; shown by MediaAdaptor when
                 a tracked anime has new available episodes since the last session. -->
            <TextView
                android:id="@+id/itemCompactNewEpisodeBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:background="@drawable/item_new_episode_badge"
                android:fontFamily="@font/poppins_bold"
                android:paddingStart="7dp"
                android:paddingTop="3dp"
                android:paddingEnd="9dp"
                android:paddingBottom="3dp"
                android:text="NEW EP"
                android:textColor="@android:color/white"
                android:textSize="10sp"
                android:visibility="gone"
                tools:ignore="SpUsage,TextContrastCheck,HardcodedText" />

            <!-- Watch progress bar — thin strip at very bottom edge of card.
                 Visible only when userProgress > 0. Width set by adapter. -->
            <ProgressBar
                android:id="@+id/itemCompactWatchProgress"
                style="@android:style/Widget.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:layout_gravity="bottom"
                android:max="100"
                android:progress="50"
                android:progressTint="?attr/colorPrimary"
                android:progressBackgroundTint="#33FFFFFF"
                android:visibility="gone"
                tools:ignore="UnusedAttribute" />
        </androidx.cardview.widget.CardView>

        <!-- Green dot — bottom-start, shown when anime is currently airing -->
        <androidx.cardview.widget.CardView
            android:id="@+id/itemCompactOngoing"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="bottom|start"
            android:translationZ="8dp"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp">

            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/item_ongoing" />
        </androidx.cardview.widget.CardView>
    </FrameLayout>

    <!-- Relation tag (sequel / prequel / etc.) — shown when applicable -->
    <LinearLayout
        android:id="@+id/itemCompactType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="4dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="gone"
        tools:ignore="UseCompoundDrawables">

        <ImageView
            android:id="@+id/itemCompactTypeImage"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:alpha="0.58"
            app:srcCompat="@drawable/ic_round_movie_filter_24"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/itemCompactRelation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:alpha="0.58"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:singleLine="true"
            android:textAllCaps="true"
            android:textSize="12sp"
            android:textStyle="italic"
            android:transitionName="mediaTitle"
            tools:ignore="TextContrastCheck"
            tools:text="Sequel" />
    </LinearLayout>
</LinearLayout>
